# Diagnostic / legacy scripts

One-off motor, encoder and alignment probes. Not used by the runtime stack.

Runtime entry points live one level up:

- `main.py` — map + web UI
- `drive_encoders.py` — Mega serial drive bridge
